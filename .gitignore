/target
/artifacts
/.sccache
node_modules/
*.tmp
.DS_Store
.vscode/
.idea/
*.swp

# property test regressions
proptest-regressions/
*.proptest-regressions

# despeckle reports (--report output)
/report/

# perf profiling artifacts (perf record / cargo flamegraph drops these in the cwd)
/perf.data
/perf.data.old
/flamegraph.svg

# Python tooling (uv-managed, ruff/mypy caches)
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.venv/
/uv.lock
/flamegraph.svg

# Real scan data is held out of the repo for copyright reasons; only the
# tiny synthetic fixtures under samples/ are tracked.
*.pdf
/private/
/scans/
/samples/private/
